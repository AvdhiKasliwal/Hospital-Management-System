HospitalSystem_Final_v2 - Ready project
---------------------------------------
1) Install dependencies:
   pip install -r requirements.txt

2) Create database and tables:
   - Open MySQL Workbench, connect as root.
   - Run schema/hospital_schema_with_data.sql

3) Run:
   python hospital_app.py

Default DB credentials (can be changed via env vars):
  DB_USER=root
  DB_PASS=Avdhi@0205
  DB_NAME=hospital_db
